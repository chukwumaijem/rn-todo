import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  form: {
    flexDirection: 'row',
    marginHorizontal: 10,
    maxHeight: 50,
    alignContent: 'center',
    justifyContent: 'center',
  },
  todoInput: {
    lineHeight: 14,
    fontSize: 14,
  },
});
